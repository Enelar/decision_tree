<b><%= this.label %></b>
<%= this.DeferRender('submenu', {}) %>
<%
this.Defer(function()
{
  this.first()    
    .parent()
    .draggable()
    .uniqueId()
    .css(
    {
      width: 'initial', 
      'background-color': 'gray'
    });
});
%>